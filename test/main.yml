---
  - name: test
    hosts: localhost
    gather_facts: no
    connection: local

    tasks:
      - name: TASK 1
        command: date
        register: time

      - name: show time
        debug:
          var: time.stdout